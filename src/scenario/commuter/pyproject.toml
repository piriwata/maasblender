[project]
name = "maasblender-commuter-scenario"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi~=0.103.2",
    "mblib",
    "pydantic~=2.4.2",
    "pydantic_settings~=2.0.3",
    "simpy~=4.1.1",
    "uvicorn~=0.23.2",
]

[tool.uv]
package = false

[tool.uv.sources]
mblib = { path = "../../../libs/mblib" }
