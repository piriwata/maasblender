[project]
name = "maasblender-dev-tools"
version = "0.0.0"
description = "Repository-wide development tools for MaaS Blender"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "ruff>=0.12.0",
]

[tool.uv]
package = false
